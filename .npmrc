# Hoist ESLint + Prettier packages to the root node_modules so Next.js's
# shared ESLint config (eslint-config-next) can resolve its plugins from
# apps/web under pnpm's strict layout.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
