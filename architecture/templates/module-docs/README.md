# <Module Name>

> Copy this folder to a module's `_docs/` and fill it in. Delete files you
> don't need yet, but keep `README.md` and `OWNERS`.

## Purpose

Why this module exists, stated from the perspective of its **consumers**
(other modules) and the **user groups** it ultimately serves. One short
paragraph. Not an implementation description.

## Public surface

What other modules may depend on (functions, components, endpoints, types).
Everything else is internal and may change without notice.

## See also

- `architecture.md` — internal structure (optional)
- `decisions.md` — module-local decisions (optional)
- `glossary.md` — module-local vocabulary (optional)
