# Claude Code Slash-Command Wrappers

Thin wrappers exposing the canonical repo-local skills in
[`.agents/skills/`](../../.agents/skills/) as Claude Code slash commands.
Keep skill logic in `.agents/skills/`; these files only reference it.

No commands defined yet (Sprint 0).
